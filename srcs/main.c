#include "fdf.h"

int main(int argc, char **argv)
{
	//t_mlx	mlx = {0};

	if (argc != 2)
		return (-1);
/*
	fdf_mlx_init(&mlx);
	mlx_on_event(mlx.mlx, mlx.win, MLX_KEYDOWN, key_hook, &mlx);
	mlx_loop(mlx.mlx);
*/
}
